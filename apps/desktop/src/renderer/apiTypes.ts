/**
 * Renderer compatibility entrypoint for Arcelle's process-boundary contract.
 * The canonical declarations live in ../shared/apiTypes.ts so main, preload,
 * renderer, and tests compile against one source of truth.
 */
export * from "../shared/apiTypes";
